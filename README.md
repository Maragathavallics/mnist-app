# mnist-app
Handwritten Digit Recognition using CNN with Streamlit web app (99.3% accuracy)
# Handwritten Digit Recognition

This project uses a Convolutional Neural Network (CNN) trained on the MNIST dataset to recognize handwritten digits (0–9).

## Features
- 99.3% test accuracy
- Draw digits using web interface
- Real-time prediction with confidence score
- Top-3 predictions displayed

## Tech Stack
- Python
- TensorFlow / Keras
- Streamlit

## How to Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
